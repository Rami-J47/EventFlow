import { BrowserRouter,Link,Route,Routes } from 'react-router-dom'; import EventsPage from './pages/EventsPage'; import RegistrationPage from './pages/RegistrationPage'; import RegistrationStatusPage from './pages/RegistrationStatusPage';
export default function App(){return <BrowserRouter><header><Link className="brand" to="/"><span>Event</span>Flow</Link><span className="header-note">Registration & ticket confirmation</span></header><Routes><Route path="/" element={<EventsPage/>}/><Route path="/events/:eventId/register" element={<RegistrationPage/>}/><Route path="/registrations/:reference" element={<RegistrationStatusPage/>}/><Route path="*" element={<main className="page-shell"><div className="state-card">Page not found.</div></main>}/></Routes></BrowserRouter>;}
