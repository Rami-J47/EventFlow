import { useEffect,useState } from 'react'; import { Link,useNavigate,useParams } from 'react-router-dom'; import RegistrationForm from '../components/RegistrationForm'; import { api } from '../services/api';
export default function RegistrationPage(){const{eventId}=useParams();const navigate=useNavigate();const[event,setEvent]=useState(null);const[error,setError]=useState('');useEffect(()=>{api.getEvent(eventId).then(setEvent).catch((e)=>setError(e.message));},[eventId]);if(error)return <main className="page-shell"><div className="state-card error">{error}</div></main>;if(!event)return <main className="page-shell"><div className="state-card">Loading event...</div></main>;return <main className="page-shell narrow"><Link className="back-link" to="/">← All events</Link><section className="form-card"><div className="eyebrow">Event registration</div><h1>{event.name}</h1><p>{event.description}</p><RegistrationForm onSubmit={async(values)=>{const registration=await api.register(event.id,values);navigate(`/registrations/${registration.registration_reference}`,{state:{justRegistered:true}});}}/></section></main>;}
